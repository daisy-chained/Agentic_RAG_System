# Project Structure, Tech Stack & Roadmap

[← Back to README](../README.md)

---

## Project Structure

The repository is a monorepo containing four distinct services (Java control plane, Python inference engine, React frontend, shared Protobuf definitions) alongside the bootstrapping and tooling scripts.

```
Agentic_RAG_System/
├── init.sh                           # First-time Ubuntu setup: drivers, deps, model selection, .env
├── start.sh                          # Local dev launcher (all services in one command)
├── build.sh                          # Regenerates gRPC stubs (Python + Java)
├── compose.yaml                      # Full containerised stack
├── .env                              # Generated by init.sh; read by compose.yaml and python-dotenv
│
├── shared-protos/
│   └── ai_service.proto              # Single source of truth for the gRPC contract
│
├── control-plane/                    # Java 21 · Spring Boot 3
│   ├── pom.xml
│   └── src/main/java/com/ai/orchestrator/
│       ├── AiClient.java             # Low-level gRPC blocking stub wrapper
│       ├── OrchestratorApplication.java
│       ├── config/
│       │   └── SecurityConfig.java   # Phase 1: permits all; replace before prod
│       ├── controller/
│       │   ├── ChatController.java   # POST /api/chat (stateful, persists history)
│       │   ├── DocumentController.java # POST/GET /api/documents
│       │   └── QueryController.java  # POST /api/v1/query (stateless debug)
│       ├── dto/
│       │   ├── ChatRequest.java
│       │   ├── QueryRequest.java
│       │   └── QueryResponse.java
│       ├── model/
│       │   ├── Conversation.java     # JPA entity: conversations table
│       │   ├── DocumentMetadata.java # JPA entity: document_metadata table
│       │   └── IndexingStatus.java   # Enum: PENDING, INDEXING, INDEXED, FAILED
│       ├── repository/
│       │   ├── ConversationRepository.java
│       │   └── DocumentMetadataRepository.java
│       └── service/
│           ├── AiQueryService.java   # Orchestrates query + chat; loads/saves history
│           └── DocumentService.java  # @Async indexing via gRPC
│
├── inference-engine/                 # Python 3.12 · asyncio gRPC server
│   ├── main.py                       # gRPC servicer: ProcessQuery + IndexDocument + Reflexion loop
│   ├── rag.py                        # Qdrant vector store + OllamaEmbeddings initialisation
│   ├── requirements.txt
│   └── Dockerfile
│
├── frontend/                         # React 18 · Vite 5 · TypeScript
│   └── src/
│       ├── App.tsx                   # Chat UI + document upload; proxies /api → :8080
│       └── index.css                 # Global styles
│
├── docs/                             # Reference documentation (this directory)
│   ├── getting-started.md
│   ├── local-development.md
│   ├── api-reference.md
│   ├── ingestion-pipeline.md
│   ├── data-model.md
│   ├── configuration.md
│   ├── observability-security.md
│   └── project-structure.md
│
└── documentation/                    # Informal project notes
    ├── idea.md                       # Original project concept notes
    └── planning.md                   # Phase-by-phase build plan
```

### Key files at a glance

| File | Purpose |
|---|---|
| `shared-protos/ai_service.proto` | The contract between Java and Python — never edit stubs directly, only edit this file and re-run `build.sh` |
| `init.sh` | One-shot Ubuntu bootstrap; run once per machine |
| `start.sh` | One-command local dev launcher |
| `build.sh` | Regenerate gRPC stubs after proto changes |
| `compose.yaml` | Full containerised stack definition |
| `.env` | Auto-generated by `init.sh`; controls model, context window, and service addresses |

---

## Tech Stack

| Layer | Technology |
|---|---|
| **Control Plane** | Java 21, Spring Boot 3.2, Spring Data JPA, Spring Security |
| **gRPC (Java)** | `grpc-client-spring-boot-starter`, `protobuf-maven-plugin` |
| **Inference Engine** | Python 3.12, `grpcio 1.62.2`, `langchain`, `langchain-ollama` |
| **Vector Store** | Qdrant, `langchain-qdrant`, `nomic-embed-text` (768-dim cosine) |
| **LLM** | Ollama — model auto-selected by `init.sh` based on available VRAM/RAM |
| **Persistence** | PostgreSQL 16, pgvector |
| **Observability** | Arize Phoenix, OpenTelemetry, OpenInference |
| **Frontend** | React 18, Vite 5, TypeScript, Lucide React |
| **Infrastructure** | Docker Compose v2 |
| **Bootstrapping** | `init.sh` (Ubuntu 20.04/22.04/24.04) |

The polyglot architecture deliberately uses each language where it is strongest: Java's type safety, thread model, and mature ecosystem handle the API gateway, persistence, and orchestration layer; Python's AI/ML ecosystem (LangChain, Ollama, Qdrant clients) handles the inference and retrieval layer. gRPC with a shared `.proto` contract keeps the boundary between the two strictly typed.

---

## Known Limitations & Roadmap

These are the known gaps in the current phase of the project. They represent intentional trade-offs made to keep the core pipeline simple and auditable during development.

| Limitation | Notes |
|---|---|
| `userId` hardcoded to `"dev1"` in the frontend | Replace with real auth (JWT/OAuth2) before multi-user use. See [Observability & Security](observability-security.md) |
| `ddl-auto: create-drop` drops the database on every restart | Add Flyway migrations and switch to `validate` for persistent data. See [Configuration](configuration.md) |
| `confidence_score` is hardcoded to `0.9` | Dynamic calculation from Qdrant cosine distance scores is planned |
| OTLP endpoint hardcoded to `localhost:4317` | Make configurable via an environment variable |
| No streaming responses | LLM outputs are buffered end-to-end; switching to `ainvoke` with an async generator would enable token-by-token streaming |
| Single Qdrant collection for all users | Per-user or per-document-set namespacing is planned to prevent cross-user retrieval leakage |
| GPU driver install may require reboot | `init.sh` emits a warning and sets a flag; subsequent runs detect the installed driver automatically |

---

## Related Pages

- [Getting Started](getting-started.md) — running the stack for the first time
- [Local Development](local-development.md) — per-service development instructions
- [Configuration](configuration.md) — environment variables and YAML settings
